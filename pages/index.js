import Hero from '../components/hero';
import Layout from '../components/layout';
import './index.scss';

const Index = () => (
  <Layout>
    <div className="hero">
      <Hero />
    </div>
  </Layout>
);

export default Index;
